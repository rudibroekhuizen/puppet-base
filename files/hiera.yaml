---
:backends:
  - yaml
  
:hierarchy:
  - global
  - defaults
  - "%{data_source}"
  - "%{clientcert}"
  - "%{environment}"

:yaml:
  :datadir: /etc/puppetlabs/puppet/hieradata

:merge_behavior: deeper
